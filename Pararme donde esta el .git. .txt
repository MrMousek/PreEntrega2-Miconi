 Pararme donde esta el .git. 

Click derecho y git bash here luego 

git add . (el punto agrega todo lo que se modifico para que se suba)

luego 

git commit -m "" (la descripción de lo que estoy subiendo. por ej: media queries, sobre nosotros.)

git push (sube todo)